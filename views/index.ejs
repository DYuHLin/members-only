<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Home</title>
</head>
<body>
    <%if(user){ %>
        <section>
            <h1>Members Only</h1>
            <h2>Welcome, <%=user.name %></h2>

            <%if(user.member === true){ %>
            <a class="page-links" href="/create">Create Post</a>
            <% } else{ %>
                <a class="page-links" href="/membership">Become a member</a>
                <a class="page-links" href="/admin">Become a admin</a>
            <%} %>
            <%posts.forEach(element => { %>
                <div class="home-page">
                    <a class="detail-links" href="<%=element.url %>"><%=element.title %></a>
                    <p><%=element.message %></p>
                    <p><%=element.date_formatted %></p>
                    <span><%=element.user.name %></span>
                </div>
            <% }); %>

            <a class="page-links log" href="/logout">Log out</a>
        </section>
    <%}else{ %>
        <section>
            <h1>Members Only</h1>
            <h2>Home</h2>
            <%posts.forEach(element => { %>
                <div class="home-page">
                    <a class="detail-links" href="<%=element.url %>"><%=element.title %></a>
                    <p><%=element.message %></p>
                    <p>Sign in to see when this was posted</p>
                    <span>Sign in to see who posted</span>
                </div>
            <% }); %>

            <a class="page-links log" href="/login">Log in</a>
        </section>
    <%} %>
    
</body>
</html>